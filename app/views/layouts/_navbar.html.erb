<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <p class="navbar-text navbar-left">
      <%= link_to 'Wikinimous', articles_path%>
    <p class="navbar-text navbar-right"><%= link_to 'New Article', new_article_path if current_user %>
      |
      <% if current_user %>
      Signed in as
      <%= current_user.name %>
      |
      <%= link_to "Logout", '/logout' %>
  <% else %>
      <%= link_to 'Login', '/login' %>
      |
      <%= link_to 'Signup', '/signup' %>
      <% end %>
  </div>
</nav>
